# @ngrx/signals has no Angular-22 release yet (latest 21.x peer-requires Angular ^21).
# Its wrapped signal primitives are stable across 21→22 and the build + unit tests pass,
# so we tolerate the peer mismatch until @ngrx/signals@22 ships, then remove this.
legacy-peer-deps=true
