export const environment = {
  production: true,
  staging: true,
  apiUrl: 'https://api-staging.edenrelics.co.uk',
  // Single gated tool instance today — staging points at the same app until a staging tool exists.
  toolApiUrl: 'https://eden-relics-tool.fly.dev',
};
